#include <stdio.h>
#include <string.h>

struct Candidate {
    char name[100];
    int count;
};

int main() {
    int n;
    scanf("%d", &n);

    struct Candidate arr[1000];
    int size = 0;

    for(int i = 0; i < n; i++) {
        char temp[100];
        scanf("%s", temp);

        int found = 0;
        for(int j = 0; j < size; j++) {
            if(strcmp(arr[j].name, temp) == 0) {
                arr[j].count++;
                found = 1;
                break;
            }
        }
